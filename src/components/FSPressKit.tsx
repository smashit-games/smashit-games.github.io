import React from 'react';

const FSPresskit: React.FC = () => {
  return (
    <div className="min-h-screen bg-darkGreen text-terminalGreen font-terminal p-8">
      <h1 className="text-4xl text-center mb-8">Fel Survivors PressKit</h1>
      {/* Add presskit content here */}
      <p>
        Welcome to the Fel Survivors PressKit page. Here you'll find all the assets and information
        you need.
      </p>
      {/* Include images, download links, etc. */}
    </div>
  );
};

export default FSPresskit;
